<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <!-- log4net configuration is now in log4net.config (loaded explicitly at startup). -->
  <!-- Assembly binding redirects are not needed in .NET 10. -->
</configuration>
